<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Winpepper.App.Views.Controls.HotkeyRecorderBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Spacing="4">
        <TextBlock x:Name="LabelBlock" Text="Hotkey" />
        <Border BorderBrush="{ThemeResource ControlElevationBorderBrush}" BorderThickness="1" CornerRadius="4" Padding="8,4">
            <Grid>
                <TextBlock x:Name="ChordText" Text="(press a chord)" Foreground="{ThemeResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" />
                <StackPanel Orientation="Horizontal" Spacing="4" HorizontalAlignment="Right">
                    <Button x:Name="RecordButton" Content="Record" Click="OnRecordClick" />
                    <Button x:Name="CancelButton" Content="Cancel" Click="OnCancelClick" Visibility="Collapsed" />
                </StackPanel>
            </Grid>
        </Border>
        <TextBlock x:Name="ErrorText" Foreground="OrangeRed" Visibility="Collapsed" />
    </StackPanel>
</UserControl>
