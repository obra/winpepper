<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Winpepper.App.Views.CorrectionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer>
        <StackPanel Padding="24" Spacing="20" MaxWidth="720">
            <TextBlock Text="Corrections" Style="{ThemeResource TitleTextBlockStyle}" />

            <StackPanel Spacing="6">
                <TextBlock Text="Preferred transcriptions" Style="{ThemeResource SubtitleTextBlockStyle}" />
                <ListView x:Name="PreferredList" AutomationProperties.AutomationId="CorrectionsPreferredList">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid ColumnDefinitions="*,Auto" Padding="4">
                                <TextBlock Text="{Binding Text}" VerticalAlignment="Center" />
                                <Button Grid.Column="1" Content="Remove" Click="OnRemovePreferred" Tag="{Binding}" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                    <TextBox x:Name="NewPreferredBox" AutomationProperties.AutomationId="CorrectionsNewPreferredTextBox" PlaceholderText="Add a preferred transcription" />
                    <Button Grid.Column="1" Content="Add" Click="OnAddPreferred" AutomationProperties.AutomationId="CorrectionsAddPreferredButton" />
                </Grid>
                <TextBlock x:Name="PreferredError" AutomationProperties.AutomationId="CorrectionsPreferredErrorLabel" Foreground="OrangeRed" />
            </StackPanel>

            <StackPanel Spacing="6">
                <TextBlock Text="Misheard replacements" Style="{ThemeResource SubtitleTextBlockStyle}" />
                <ListView x:Name="ReplacementsList" AutomationProperties.AutomationId="CorrectionsReplacementsList">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid ColumnDefinitions="*,*,Auto" Padding="4" ColumnSpacing="8">
                                <TextBlock Text="{Binding Wrong}" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1" Text="{Binding Right}" VerticalAlignment="Center" />
                                <Button Grid.Column="2" Content="Remove" Click="OnRemoveReplacement" Tag="{Binding}" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <Grid ColumnDefinitions="*,*,Auto" ColumnSpacing="8">
                    <TextBox x:Name="NewWrongBox" AutomationProperties.AutomationId="CorrectionsNewWrongTextBox"  PlaceholderText="wrong (heard)" />
                    <TextBox x:Name="NewRightBox" AutomationProperties.AutomationId="CorrectionsNewRightTextBox"  Grid.Column="1" PlaceholderText="right (correct)" />
                    <Button x:Name="AddReplacementButton" AutomationProperties.AutomationId="CorrectionsAddReplacementButton" Grid.Column="2" Content="Add" Click="OnAddReplacement" />
                </Grid>
                <TextBlock x:Name="ReplacementsError" AutomationProperties.AutomationId="CorrectionsReplacementsErrorLabel" Foreground="OrangeRed" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
