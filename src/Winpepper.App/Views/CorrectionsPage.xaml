<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Winpepper.App.Views.CorrectionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer>
        <StackPanel Padding="24" Spacing="12" MaxWidth="720" HorizontalAlignment="Left">

            <TextBlock Text="Corrections" Style="{ThemeResource TitleTextBlockStyle}" />
            <TextBlock Text="Teach Winpepper the words you actually use, and fix what it keeps mishearing."
                       Style="{ThemeResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       TextWrapping="Wrap"
                       Margin="0,0,0,8" />

            <!-- Preferred transcriptions -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel Spacing="12">
                    <TextBlock Text="Preferred transcriptions" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                    <TextBlock Text="Names and terms the recognizer should favor."
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <ListView x:Name="PreferredList" AutomationProperties.AutomationId="CorrectionsPreferredList" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid ColumnDefinitions="*,Auto" Padding="0,4">
                                    <TextBlock Text="{Binding Text}" VerticalAlignment="Center" />
                                    <Button Grid.Column="1" Content="Remove" Click="OnRemovePreferred" Tag="{Binding}" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                        <TextBox x:Name="NewPreferredBox" AutomationProperties.AutomationId="CorrectionsNewPreferredTextBox" PlaceholderText="Add a preferred transcription" />
                        <Button Grid.Column="1" Content="Add" Click="OnAddPreferred" AutomationProperties.AutomationId="CorrectionsAddPreferredButton" Style="{StaticResource AccentButtonStyle}" />
                    </Grid>
                    <TextBlock x:Name="PreferredError"
                               AutomationProperties.AutomationId="CorrectionsPreferredErrorLabel"
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                               TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <!-- Misheard replacements -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel Spacing="12">
                    <TextBlock Text="Misheard replacements" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                    <TextBlock Text="When the left text is heard, it is replaced with the right text."
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <Grid ColumnDefinitions="*,*,Auto" ColumnSpacing="8">
                        <TextBox x:Name="NewWrongBox" AutomationProperties.AutomationId="CorrectionsNewWrongTextBox"  PlaceholderText="wrong (heard)" />
                        <TextBox x:Name="NewRightBox" AutomationProperties.AutomationId="CorrectionsNewRightTextBox"  Grid.Column="1" PlaceholderText="right (correct)" />
                        <Button x:Name="AddReplacementButton" AutomationProperties.AutomationId="CorrectionsAddReplacementButton" Grid.Column="2" Content="Add" Click="OnAddReplacement" Style="{StaticResource AccentButtonStyle}" />
                    </Grid>
                    <TextBlock x:Name="ReplacementsError"
                               AutomationProperties.AutomationId="CorrectionsReplacementsErrorLabel"
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                               TextWrapping="Wrap" />
                    <ListView x:Name="ReplacementsList" AutomationProperties.AutomationId="CorrectionsReplacementsList" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid ColumnDefinitions="*,*,Auto" Padding="0,4" ColumnSpacing="8">
                                    <TextBlock Text="{Binding Wrong}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1" Text="{Binding Right}" VerticalAlignment="Center" />
                                    <Button Grid.Column="2" Content="Remove" Click="OnRemoveReplacement" Tag="{Binding}" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
