<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Winpepper.App.Views.RecordingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Winpepper.App.Views.Controls">
    <ScrollViewer>
        <StackPanel Padding="24" Spacing="12" MaxWidth="720" HorizontalAlignment="Left">

            <TextBlock Text="Recording" Style="{ThemeResource TitleTextBlockStyle}" />
            <TextBlock Text="Set up how you start and stop dictation, and which microphone Winpepper listens to."
                       Style="{ThemeResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       TextWrapping="Wrap"
                       Margin="0,0,0,8" />

            <!-- Hotkeys -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel Spacing="12">
                    <TextBlock Text="Hotkeys" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                    <TextBlock Text="Global shortcuts that work in any app."
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <controls:HotkeyRecorderBox x:Name="HoldBox" Label="Hold to record" AutomationProperties.AutomationId="RecordingHoldHotkeyBox" />
                    <controls:HotkeyRecorderBox x:Name="ToggleBox" Label="Toggle to record" AutomationProperties.AutomationId="RecordingToggleHotkeyBox" />
                </StackPanel>
            </Border>

            <!-- Microphone -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel Spacing="12">
                    <TextBlock Text="Microphone" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                    <ComboBox x:Name="MicCombo" AutomationProperties.AutomationId="RecordingMicCombo" PlaceholderText="Select an input device" HorizontalAlignment="Stretch" />
                    <ProgressBar x:Name="LevelMeter" AutomationProperties.AutomationId="RecordingLevelMeter" Minimum="0" Maximum="1" Height="6" />
                    <TextBlock Text="Speak and the level meter should move."
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </StackPanel>
            </Border>

            <!-- Options -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel Spacing="8">
                    <TextBlock Text="Options" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                    <ToggleSwitch x:Name="SoundsToggle" AutomationProperties.AutomationId="RecordingSoundsToggle" Header="Play start/stop sounds" />
                    <ToggleSwitch x:Name="SpeakerFilterToggle" AutomationProperties.AutomationId="RecordingSpeakerFilterToggle" Header="Speaker filter (experimental)" />
                    <ToggleSwitch x:Name="PostPasteLearningToggle" AutomationProperties.AutomationId="RecordingPostPasteLearningToggle" Header="Offer to learn corrections after typing" />
                    <ToggleSwitch x:Name="PrewarmMicToggle" AutomationProperties.AutomationId="RecordingPrewarmMicToggle" Header="Keep the mic warm to capture the start of speech (mic indicator stays on)" ToolTipService.ToolTip="Uses a little extra CPU and battery to keep the mic ready; the mic-in-use indicator stays on." />
                    <TextBlock TextWrapping="Wrap"
                               Text="Keeps one microphone stream open the whole time Winpepper runs (the mic-in-use indicator stays on and it uses a little CPU and battery). Turn it off to only use the mic while dictating."
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="After Winpepper types, watch for an edit you make and offer to remember it as a correction. Off by default."
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <ToggleSwitch x:Name="AutostartToggle" AutomationProperties.AutomationId="RecordingAutostartToggle" Header="Start with Windows" />
                </StackPanel>
            </Border>

            <!-- Test -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel Spacing="12">
                    <TextBlock Text="Test dictation" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                    <TextBlock Text="Focus the box, press your hotkey, and say a sentence."
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TestBox" AutomationProperties.AutomationId="RecordingTestTextBox" PlaceholderText="Test dictation lands here..." />
                        <Button Grid.Column="1" Content="Focus" Click="OnFocusTestBox" AutomationProperties.AutomationId="RecordingFocusTestBoxButton" VerticalAlignment="Bottom" />
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Speech recognition -->
            <StackPanel Spacing="8" Margin="0,16,0,0">
                <TextBlock Text="Speech recognition" Style="{StaticResource SubtitleTextBlockStyle}" />

                <ComboBox x:Name="AsrProviderCombo" Header="Provider" MinWidth="280">
                    <ComboBoxItem Content="Local processing (on this PC)" Tag="local" />
                    <ComboBoxItem Content="AssemblyAI (cloud)" Tag="assemblyai" />
                </ComboBox>

                <TextBlock
                    x:Name="AsrPrivacyText"
                    Text="Cloud transcription sends your recorded audio to AssemblyAI. Winpepper asks AssemblyAI to delete your audio and transcript after transcription (deletion happens on AssemblyAI's servers and may not be immediate). Turn deletion off below to keep them per AssemblyAI's retention policy."
                    TextWrapping="Wrap"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Style="{StaticResource CaptionTextBlockStyle}" />

                <StackPanel x:Name="AssemblyAiPanel" Spacing="8" Margin="0,4,0,0">
                    <PasswordBox x:Name="AssemblyAiKeyBox" Header="API key" MinWidth="280"
                                 PlaceholderText="Paste your AssemblyAI API key" />
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button x:Name="SaveKeyButton" Content="Save key" />
                        <Button x:Name="ClearKeyButton" Content="Clear key" />
                        <Button x:Name="TestKeyButton" Content="Test" />
                    </StackPanel>
                    <ComboBox x:Name="AssemblyAiModelCombo" Header="Model" MinWidth="280" />
                    <TextBox x:Name="AssemblyAiModelBox" Header="Custom model id" MinWidth="280"
                             Visibility="Collapsed"
                             PlaceholderText="Advanced: exact AssemblyAI speech_model id" />
                    <TextBlock x:Name="AssemblyAiModelWarning"
                               Visibility="Collapsed"
                               Text="Custom model ids are not validated and will fail at dictation time if wrong."
                               TextWrapping="Wrap"
                               Foreground="{ThemeResource SystemFillColorCautionBrush}"
                               Style="{StaticResource CaptionTextBlockStyle}" />
                    <ToggleSwitch x:Name="AssemblyAiDeleteToggle" Header="Delete audio from AssemblyAI after transcription" />
                    <ToggleSwitch x:Name="AssemblyAiKeytermsToggle" Header="Send preferred terms as keyterms" />
                    <TextBlock Text="Preferred terms may incur extra AssemblyAI cost on some plans. Off by default. Your corrections list is always applied via custom spelling at no extra cost."
                               TextWrapping="Wrap"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Style="{StaticResource CaptionTextBlockStyle}" />
                    <TextBlock x:Name="AsrStatusText"
                               TextWrapping="Wrap"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Style="{StaticResource CaptionTextBlockStyle}" />
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
